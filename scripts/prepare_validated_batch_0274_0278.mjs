import fs from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { createHash } from "node:crypto";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const work = "/home/ubuntu/dontonyo-work/batch-0274-0278";
const out = path.join(root, "supabase", "batch-0274-0278");
const q = value => `'${String(value ?? "").replaceAll("'", "''")}'`;
const j = value => `${q(JSON.stringify(value))}::jsonb`;
const h = value => createHash("sha256").update(value).digest("hex");
export const BATCH_PAGES = [274, 275, 276, 277, 278];
export const PIPELINE_VERSION = "local-ocr-quality-gated-recovery-batch-0274-0278-v1";

export async function buildBatch() {
  const content = JSON.parse(await fs.readFile(path.join(out, "approved_content.json"), "utf8"));
  const pages = await Promise.all(BATCH_PAGES.map(async page => ({ page, sha256: h(await fs.readFile(path.join(work,"rendered",`page-${page}.jpg`))), raw: await fs.readFile(path.join(work,"ocr",`page-0${page}.txt`),"utf8") })));
  const audit = { batch_pages:BATCH_PAGES, pipeline_version:PIPELINE_VERSION, generated_facts:0, generated_notes:0, generated_mcqs:0, generated_options:0, generated_flashcards:0, source_pages:pages.map(page=>({page:page.page,review_status:"completed_image_grounded_review",image_sha256:page.sha256,overall_confidence:"medium"})), verification_counts:{verified:0,conflicting:0,source_attributed:0}, source_anomalies:["All 35 ordered overlap-safe recovery tiles were reviewed.","Local Bangla-English OCR was reconciled with visual evidence; image evidence controlled.","OCR degradation, printed source-correction notes, constitutional references, names, captions, and numerical values were preserved without silent correction.","National, constitutional, political, legal, government, historical, religious, ideological, institutional, human-rights, environmental, gender-related, source-corrected, named-person, image-caption, numeric, design/measurement, and date-sensitive claims were withheld."], quality_gates:["Only recovery source pages 274–278 are included.","No reviewed claim clears the conservative quality, policy-safety, internal-consistency, source-correction, and authority-verification boundary.","Only reviewed source provenance and page-local topic boundaries are admitted."] };
  const topicSql = content.topics.map(t=>`INSERT INTO public.topics (chapter_id,title,slug,description,source_page,display_order) SELECT c.id,${q(t.title)},${q(t.slug)},'Source-derived content with completed visual review, conservative classification, and recorded verification decision.',${t.page},${t.order} FROM public.chapters c JOIN public.books b ON b.id=c.book_id WHERE b.title='Jubayer''s GK' AND c.slug='europe' AND NOT EXISTS (SELECT 1 FROM public.topics x WHERE x.chapter_id=c.id AND x.slug=${q(t.slug)});`).join("\n");
  const sourceSql = pages.map(p=>{ const slug=content.topics.find(t=>t.page===p.page).slug; const metadata={physical_source_page:p.page,source_image_sha256:p.sha256,review_status:"completed_image_grounded_review",review_report:`${out}/visual_review_274_278.md`,classification_report:`${out}/classification_decisions.md`,verification_report:`${out}/external_verification.md`,orientation_note:"Rendered source image visually verified upright before local OCR."}; return `INSERT INTO public.source_pages (import_run_id,book_id,source_page,page_kind,raw_transcription,chapter_heading,topic_heading,confidence,extraction_method,model_name,notes,review_metadata) SELECT (SELECT id FROM public.import_runs WHERE pipeline_version=${q(PIPELINE_VERSION)} ORDER BY started_at DESC LIMIT 1),b.id,${p.page},'educational'::page_kind,${q(p.raw)},'Bangladesh constitutional-reference source boundary',${q(slug)},'medium'::confidence_level,'local_ocr_with_image_grounded_review','tesseract-ben+eng','All ordered source tiles reviewed; upright OCR reconciled with visual evidence; no learner-facing records classified for import.',${j(metadata)} FROM public.books b WHERE b.title='Jubayer''s GK' AND NOT EXISTS (SELECT 1 FROM public.source_pages s WHERE s.book_id=b.id AND s.source_page=${p.page});`; }).join("\n");
  const tags=[["source-provenance","Source provenance","quality","Source-preserved record without learner-facing claim admission."],["bangladesh-constitutional-reference","Bangladesh constitutional reference","domain","Constitutional material retained as reviewed source provenance."],["sensitive-history-reference","Sensitive history reference","domain","Sensitive historical material retained as reviewed source provenance."],["source-correction-boundary","Source correction boundary","quality","Printed source corrections retained without claim admission."]];
  const sql=`-- Generated by prepare_validated_batch_0274_0278.mjs. Recovery source pages 274–278 only.\nBEGIN;\nINSERT INTO public.import_runs (source_filename,source_sha256,pipeline_version,status,completed_at,audit) SELECT 'Jubayer''sgk.pdf',${q(h(pages.map(p=>p.sha256).join("|")))},${q(PIPELINE_VERSION)},'completed',now(),${j(audit)} WHERE NOT EXISTS (SELECT 1 FROM public.import_runs WHERE pipeline_version=${q(PIPELINE_VERSION)});\n${topicSql}\n${sourceSql}\nINSERT INTO public.content_tags (slug,label,category,description) VALUES ${tags.map(r=>`(${r.map(q).join(",")})`).join(",")} ON CONFLICT (slug) DO UPDATE SET label=EXCLUDED.label,category=EXCLUDED.category,description=EXCLUDED.description;\nCOMMIT;`;
  return {sql,audit,counts:{batch_pages:BATCH_PAGES,review_tiles:35,eligible_facts:0,eligible_notes:0,eligible_mcqs:0,eligible_mcq_options:0,withheld_mcqs:0,derived_records:0,verification_statuses:audit.verification_counts}};
}
if(process.argv[1]===fileURLToPath(import.meta.url)){const {sql,audit,counts}=await buildBatch();await fs.mkdir(out,{recursive:true});await fs.writeFile(path.join(out,"validated_import.sql"),sql);await fs.writeFile(path.join(out,"batch_audit.json"),JSON.stringify(audit,null,2));await fs.writeFile(path.join(out,"execute_sql_request.json"),JSON.stringify({project_id:"rennotctgrxvbpghbimx",query:sql}));await fs.writeFile(path.join(work,"import_input_counts.json"),JSON.stringify(counts,null,2));}
