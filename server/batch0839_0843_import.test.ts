import { describe, expect, it } from "vitest"; import { BATCH_PAGES, PIPELINE_VERSION, buildBatch } from "../scripts/prepare_validated_batch_0839_0843.mjs";
describe("validated pages 839–843 provenance-only import contract",()=>{it("locks range and zero-admission totals",async()=>{const {audit,counts}=await buildBatch();expect(BATCH_PAGES).toEqual([839,840,841,842,843]);expect(audit.pipeline_version).toBe(PIPELINE_VERSION);expect(audit.source_pages).toHaveLength(5);expect(counts).toMatchObject({review_tiles:35,eligible_facts:0,eligible_notes:0,eligible_mcqs:0,eligible_mcq_options:0,derived_records:0});});it("retains only provenance boundaries with idempotency",async()=>{const {sql}=await buildBatch();expect(sql).toContain("BEGIN;");expect(sql).toContain("COMMIT;");expect(sql).toContain("batch-0839-0843");expect(sql).toContain("WHERE NOT EXISTS (SELECT 1 FROM public.import_runs");expect(sql).not.toContain("INSERT INTO public.gk_facts");expect(sql).not.toContain("INSERT INTO public.gk_notes");expect(sql).not.toContain("INSERT INTO public.gk_mcqs");});});
