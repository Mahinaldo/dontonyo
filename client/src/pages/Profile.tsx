import { LogIn, LogOut, UserRound } from "lucide-react";
import { startLogin } from "@/const";
import { useAuth } from "@/_core/hooks/useAuth";

export default function Profile(){const {user,loading,isAuthenticated,logout}=useAuth();if(loading)return <div className="brutal-card p-8 font-bold">Opening your account…</div>;if(!isAuthenticated)return <div className="mx-auto max-w-2xl py-12 text-center"><div className="brutal-card bg-sky p-8"><UserRound className="mx-auto size-10"/><p className="eyebrow mt-5">Your account</p><h1 className="page-title mt-4 text-5xl">MAKE STUDY<br/>PORTABLE.</h1><p className="mx-auto mt-5 max-w-lg font-medium leading-7">Sign in to keep your real study trail close — chapters, flashcard decisions, and quiz history stay private to you.</p><button onClick={()=>startLogin()} className="brutal-button mt-7 px-5 py-3"><LogIn className="size-4"/> Sign in</button></div></div>;return <div className="space-y-8"><header><p className="eyebrow">04 / Your account</p><h1 className="page-title mt-3">YOUR STUDY<br/><span className="text-[#ed4f77]">CORNER.</span></h1></header><section className="brutal-card max-w-2xl bg-mint p-6 sm:p-8"><div className="flex items-center gap-5"><div className="grid size-16 place-items-center border-2 border-black bg-pink text-2xl font-bold shadow-[4px_4px_0_#111]">{(user?.name?.[0]??"D").toUpperCase()}</div><div><p className="mono text-[10px]">SIGNED IN</p><h2 className="mt-1 text-2xl font-bold tracking-[-.06em]">{user?.name??"Dontonyo learner"}</h2><p className="mt-1 text-sm font-medium">{user?.email??"Secure account session"}</p></div></div><div className="mt-8 border-t-2 border-black pt-6"><p className="font-medium leading-7">Preferences and daily-goal controls will appear here once they are backed by real profile data. No pretend switches or filler settings.</p><button onClick={()=>logout()} className="brutal-button brutal-button-light mt-6 px-4 py-3"><LogOut className="size-4"/> Sign out</button></div></section></div>}
