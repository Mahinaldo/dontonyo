import { ArrowRight, BookOpen, Brain, Search, Target, X } from "lucide-react";
import { Link, useLocation } from "wouter";
import { useEffect, useMemo, useState } from "react";
import { trpc } from "@/lib/trpc";

const colors = ["bg-lemon", "bg-mint", "bg-pink", "bg-lilac", "bg-sky", "bg-orange"];

export default function Learn() {
  const [location, setLocation] = useLocation();
  const searchParam = useMemo(() => new URLSearchParams(location.split("?")[1] ?? "").get("search") ?? "", [location]);
  const [query, setQuery] = useState(searchParam);
  useEffect(() => setQuery(searchParam), [searchParam]);
  const library = trpc.gk.library.useQuery({ page: 1, pageSize: 24 });
  const results = trpc.gk.search.useQuery({ query: searchParam, page: 1, pageSize: 20 }, { enabled: Boolean(searchParam) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); setLocation(query.trim() ? `/learn?search=${encodeURIComponent(query.trim())}` : "/learn"); };
  return <div className="space-y-9">
    <header className="grid gap-5 border-b-[3px] border-black pb-8 lg:grid-cols-[1fr_auto] lg:items-end">
      <div><p className="eyebrow">01 / The Source Library</p><h1 className="page-title mt-3">LEARN THE<br /><span className="text-[#ed4f77]">BIG PICTURE.</span></h1><p className="mt-4 max-w-2xl font-medium leading-7 text-muted-foreground">Browse real chapters from the imported GK source. Every record stays tied to its original source page.</p></div>
      <div className="border-2 border-black bg-mint px-4 py-3 font-bold shadow-[4px_4px_0_#111]"><span className="mono text-[10px]">LIVE INDEX</span><p className="mt-1 text-2xl tracking-[-.06em]">{library.data?.total ?? "—"} chapters</p></div>
    </header>
    <form className="brutal-card p-4 sm:p-5" onSubmit={submit}><label className="mono mb-3 block text-[10px] uppercase tracking-[.12em]">Search facts, notes, questions or source labels</label><div className="flex gap-3"><div className="relative min-w-0 flex-1"><Search className="absolute left-3 top-1/2 size-5 -translate-y-1/2"/><input className="brutal-input pl-11" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search in Bangla or English..." /></div><button className="brutal-button px-5" type="submit">Search</button>{searchParam && <button type="button" onClick={()=>setLocation("/learn")} className="brutal-button brutal-button-light px-3"><X className="size-4"/></button>}</div></form>
    {searchParam ? <section><div className="mb-5 flex items-end justify-between"><div><p className="eyebrow">Search result / source preview</p><h2 className="section-title mt-2">“{searchParam}”</h2><p className="mt-3 max-w-xl text-sm font-medium leading-6 text-muted-foreground">Use previews to locate the right subject, then return to the chapter path for a structured study session.</p></div><span className="mono text-[10px]">SERVER-SIDE</span></div>{results.isLoading ? <LoadingRows/> : results.data?.items.length ? <div className="grid gap-3">{results.data.items.map(item=><article key={`${item.entityType}-${item.entityId}`} className="brutal-card-sm p-5"><div className="flex items-start justify-between gap-3"><div><span className="mono bg-lemon px-2 py-1 text-[9px] font-bold uppercase">{item.entityType}</span><h3 className="bangla mt-4 text-lg font-bold">{item.title ?? "Source record"}</h3></div></div><p className="bangla mt-3 line-clamp-3 text-sm leading-7 text-muted-foreground">{item.body}</p>{item.englishMetadata && <p className="mono mt-4 text-[10px] text-muted-foreground">{item.englishMetadata}</p>}</article>)}</div> : <Empty text="No matching source records. Try a different word or phrase."/>}</section> : <section><div className="grid gap-4 lg:grid-cols-[.75fr_1.25fr]"><div><p className="eyebrow">Three useful routes</p><h2 className="section-title mt-2">STUDY WITH<br/>A PURPOSE.</h2><p className="mt-4 max-w-sm font-medium leading-7 text-muted-foreground">Choose the learning mode that matches what you need right now—study source blocks, recall cards, or question practice.</p></div><div className="grid gap-3 sm:grid-cols-3"><Link href="#chapter-shelf" className="brutal-card-sm bg-sky p-4"><BookOpen className="size-5"/><p className="mt-7 font-bold">Study a chapter</p><p className="mt-2 text-xs font-medium">Structured source passes</p></Link><Link href="/learn/flashcards" className="brutal-card-sm bg-mint p-4"><Brain className="size-5"/><p className="mt-7 font-bold">Recall cards</p><p className="mt-2 text-xs font-medium">Rate and revisit</p></Link><Link href="/practice" className="brutal-card-sm bg-lemon p-4"><Target className="size-5"/><p className="mt-7 font-bold">Practice set</p><p className="mt-2 text-xs font-medium">Correct and review</p></Link></div></div><div id="chapter-shelf" className="mb-5 mt-10"><p className="eyebrow">Open a chapter</p><h2 className="section-title mt-2">THE GK SHELF.</h2></div>{library.isLoading ? <LoadingRows/> : <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{library.data?.items.map((chapter,index)=><Link key={chapter.id} href={`/learn/chapter/${chapter.id}`} className="brutal-card-sm hover-lift block p-5"><div className={`mb-8 grid size-12 place-items-center border-2 border-black text-sm font-bold ${colors[index%colors.length]}`}>{String(chapter.chapterNumber).padStart(2,"0")}</div><h3 className="bangla text-xl font-bold leading-tight">{chapter.title}</h3><p className="mt-3 min-h-10 text-sm font-medium text-muted-foreground">{chapter.description ?? "Source-organized learning material."}</p><span className="mt-6 flex items-center gap-2 text-xs font-bold uppercase tracking-[.08em]">Open study path <ArrowRight className="size-4"/></span></Link>)}</div>}</section>}
  </div>;
}

export function Empty({ text }: { text: string }) { return <div className="brutal-card bg-lemon p-6"><BookOpen className="mb-4 size-7"/><p className="font-bold">Nothing to show here yet.</p><p className="mt-2 max-w-xl text-sm font-medium leading-6">{text}</p></div>; }
export function LoadingRows() { return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{Array.from({length:6}).map((_,i)=><div key={i} className="h-52 animate-pulse border-2 border-black bg-muted"/>)}</div>; }
